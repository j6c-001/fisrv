name: fisrv
description: FIS Race Visualizer

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  http: ^0.12.2
  html: ^0.14.0

  flame: ^1.0.0-rc5

  mobx: ^1.2.1+2
  flutter_mobx: ^1.1.0+2
  provider: ^4.3.2+1

  youtube_player_iframe: 1.1.0


dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^1.7.2
  mobx_codegen: ^1.1.0+1

flutter:

  uses-material-design: true
  assets:
    - assets/images/flags/

